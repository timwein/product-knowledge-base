FROM python:3.12-slim

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1 \
    PIP_NO_CACHE_DIR=1

WORKDIR /app

COPY pyproject.toml ./
RUN pip install --no-cache-dir .

COPY sentinel_curator ./sentinel_curator

EXPOSE 8000

CMD ["uvicorn", "sentinel_curator.api:app", "--host", "0.0.0.0", "--port", "8000"]
